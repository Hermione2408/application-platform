import { createSlice } from "@reduxjs/toolkit";

const jobSlice = createSlice(
    {
        name:"",
        initialState:[],
        reducers:{
            
        }
    }
)
export default jobSlice.reducer